# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Images (if you want to keep them in git, remove this. But usually large binaries are managed differently or included if necessary for deployment)
# Since the user wants to use images in the current directory, I will NOT ignore the 'images' and 'public/images' folders.
# images/
# public/images/
